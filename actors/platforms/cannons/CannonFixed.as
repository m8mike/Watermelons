package {
	
	/**
	 * ...
	 * @author ...
	 */
	public class CannonFixed extends CannonSuper {
		
		public function CannonFixed(x:Number, y:Number, angle:Number = 0) {
			super(x, y);
			setAngle(angle);
		}
	}
}