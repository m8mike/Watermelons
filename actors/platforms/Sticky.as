package {
	import flash.geom.Point;
	
	/**
	 * platform that lets player slide on its side
	 * @author Mad Mike
	 */
	public class Sticky extends Platform {
		
		public function Sticky(x:Number, y:Number, w:Number = 1, h:Number = 1) {
			super(x, y, w, h);
		}
	}
}